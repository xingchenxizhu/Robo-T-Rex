# 机器霸王龙尾巴-身体协同实验 · 依赖
#
# 本机实测版本（Python 3.11.15）：
#   mujoco 3.13.0 / gymnasium 1.3.0 / stable-baselines3 2.9.0
#   torch 2.14.0+cpu / numpy 2.4.6 / matplotlib / flask / pillow / imageio
#
# 安装：
#   uv pip install --python .venv\Scripts\python.exe -r requirements.txt
# 或
#   .venv\Scripts\python.exe -m pip install -r requirements.txt

mujoco>=3.3,<4
gymnasium>=1.0,<2
stable-baselines3>=2.5,<3
torch>=2.3
numpy>=1.26
matplotlib>=3.8
flask>=3.0
pillow>=10.0
imageio>=2.34
imageio-ffmpeg>=0.5
tensorboard>=2.16
pandas>=2.0
